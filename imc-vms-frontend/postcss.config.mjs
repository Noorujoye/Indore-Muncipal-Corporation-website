/**
 * Local PostCSS config.
 *
 * This project doesn't use Tailwind yet.
 * Having this file ensures Vite/PostCSS doesn't accidentally pick up a global
 * config elsewhere on your machine (e.g. D:\\postcss.config.mjs).
 */
export default {
  plugins: {},
};
